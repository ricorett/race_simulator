#include "Air_race.h"

void Air_race::registerVehicle(){
//
}

/*
 * может сделать структуру, где каждый участник гонки будет привязан к своему номеру, а номер будет привязан к времени.
 * после подсчёта времени, идет сортировка по самому большому времени, после чего порядок номеров передается в таблицу
 * с именами участников гоник и в порядке номеров идет сортировка участников и отдается на вывод.
 * */